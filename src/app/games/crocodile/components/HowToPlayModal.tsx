"use client";

import { Modal } from "shared/ui/Modal";

export function HowToPlayModal({
  open,
  onClose,
}: {
  open: boolean;
  onClose: () => void;
}) {
  return (
    <Modal open={open} onClose={onClose} title="Как играть в Крокодил">
      <ol className="list-decimal pl-6 space-y-2 text-sm">
        <li>
          Разделитесь на 2+ команд. Ведущий показывает слово жестами, не
          произнося и не рисуя.
        </li>
        <li>
          Команда ведущего пытается угадать слово. За отгадку — очки. Сложные
          слова дают больше очков (если включено).
        </li>
        <li>
          Можно пропускать слова, но есть лимит на ход. При включённом штрафе —
          пропуск даёт -1 очко.
        </li>
        <li>
          Раунд идёт по таймеру. После истечения времени ход переходит следующей
          команде.
        </li>
        <li>
          Ведущий внутри команды ротируется автоматически (очередь ведущих
          редактируйте в сайдбаре).
        </li>
        <li>
          Побеждает команда, достигшая цели по очкам. Горячие клавиши: пробел —
          «Отгадали», → — «Пропуск», Enter — завершить/старт, H —
          показать/скрыть слово.
        </li>
      </ol>
    </Modal>
  );
}
